version: '3.8'

services:
  neo4j:
    image: neo4j:5
    container_name: neo4j_histo
    ports:
      - "7474:7474"  # HTTP UI
      - "7687:7687"  # Bolt
    environment:
      - NEO4J_AUTH=neo4j/password
    volumes:
      - ./neo4j_data:/data
    restart: unless-stopped
