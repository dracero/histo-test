{
  "name": "rag-histologia-neo4j-fullstack",
  "version": "4.1.0",
  "description": "Fullstack Histology RAG with Neo4j + A2UI",
  "scripts": {
    "dev": "docker compose up -d && uv run uvicorn server:app --reload --host 0.0.0.0 --port 10005",
    "start": "docker compose up -d && uv run python server.py"
  },
  "author": "",
  "license": "ISC"
}
